{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "gcc",
            "args": [
                "-g",
                "${workspaceFolder}/*.c",
                "-o",
                "${workspaceFolder}/main",
                "-lreadline"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
